name: round-trip-no-debug

on:
  push:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  nodebug:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        java: ['8', '17', '25']
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: '${{ matrix.java }}'
      - uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: npm
      - run: npm ci
      - run: npm run build
      - run: node test/run-e2e.mjs --no-debug
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: nodebug-${{ strategy.job-index }}
          path: /tmp/jsd-e2e-*
          if-no-files-found: ignore
          retention-days: 7
