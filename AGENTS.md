# 仓库开发规范

适用于整个仓库，以下要求必须遵守。

1. **反编译正确性第一。** 所有缺陷修复和新增功能都必须优先保证与原始字节码的 JVM 语义一致，包括求值顺序、副作用、类型、异常和可观察的元数据。不得为可读性、性能或新语法牺牲语义；仅能编译通过不代表正确。
2. **优先兼容 JDK 8u 及更早版本。** 优先处理这些版本的反编译问题，优先补齐兼容性与回归测试；支持新版 JDK 不得破坏旧版兼容性。测试样例和生成语法须符合对应目标版本。
3. **不新增代码注释。** 包括行注释、块注释、JSDoc 和 JavaDoc；用命名、类型和函数拆分表达意图，说明写入文档。保留许可证声明及具有验证用途的测试内容。
4. **修复和新功能必须有 CI 测试。** 自动化测试须接入 `.github/workflows/` 实际执行的入口，修复须有复现原问题的回归用例。引擎改动须对比原始程序与反编译后重新编译程序的语义，覆盖受影响的 JDK 及带调试、无调试模式。运行相关测试和必要的全量回归，不得跳过失败用例或放宽断言。
5. **代码写完必须格式化。** 新增或修改 TypeScript、Java 后执行 `npm run format`，并通过 `npm run format:check` 和 `npm run check`。特殊测试输入沿用仓库专门验证方式，不得破坏测试含义。
6. **禁止信息泄露。** 检查全部待提交内容及构建产物，代码、测试、文档、日志和 source map 不得泄露个人信息、凭据、内部地址或系统敏感绝对路径。使用相对路径、配置或运行时临时目录及虚构测试数据；报告问题时不复述敏感值，保留必要的公开项目标识和许可证。
7. **同步简洁的双语 changelog。** 每项修复和功能完成后更新 `CHANGELOG.md` 未发布版本，沿用 `changelog:` 与 `更新内容:`，中英文逐条对应，每个条目只描述一项用户可观察的变化，每行不超过 100 个字符；不同事项必须拆成独立条目，合并重复条目；不擅改版本号或发布日期。
8. **同步双语 README 和 demo。** 引擎功能或修复完成后执行 `npm run sync`，按实际构建产物同步浏览器包、双语 README 和 demo 全部体积数据，并通过 `npm run docs:check`。使用十进制 KB/MB、gzip 级别 9 和统一取整规则；API、用法及测试数量变化也须同步双语说明。
9. **Markdown 文档仅允许位于仓库根目录。** 其他任何目录均不得新增或保留 `.md` 文件，不区分扩展名大小写。README 同样只能位于根目录，包含无扩展名、不同扩展名及语言变体。子目录需要说明文档时，在根目录使用描述具体用途的文件名，并同步更新引用；完成前检查所有仓库文件，包括新增且未跟踪的文件。

完成前检查全部未提交改动，汇报实际验证命令和结果，明确区分本地通过、已接入 CI 与远端 CI 通过；未执行的检查不得声称通过。未经要求不提交、推送或发布。
